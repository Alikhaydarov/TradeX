name: Apply journal account split

on:
  push:
    branches:
      - agent/app-router-ui-parity-v2
    paths:
      - "tools/apply-journal-account-split.mjs"
      - ".github/workflows/apply-journal-account-split.yml"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout refactor branch
        uses: actions/checkout@v4
        with:
          ref: agent/app-router-ui-parity-v2
          fetch-depth: 0

      - name: Apply extraction codemod
        run: node tools/apply-journal-account-split.mjs

      - name: Commit extracted module wiring
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "refactor: wire extracted journal account module"
          git push origin HEAD:agent/app-router-ui-parity-v2
