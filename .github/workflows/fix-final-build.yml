name: Fix final build

on:
  push:
    branches:
      - agent/app-router-ui-parity-v2
    paths:
      - "tools/fix-final-build.mjs"
      - ".github/workflows/fix-final-build.yml"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout refactor branch
        uses: actions/checkout@v4
        with:
          ref: agent/app-router-ui-parity-v2
          fetch-depth: 0

      - name: Apply final build fix
        run: node tools/fix-final-build.mjs

      - name: Commit final build fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "fix: complete final production build cleanup"
          git push origin HEAD:agent/app-router-ui-parity-v2
