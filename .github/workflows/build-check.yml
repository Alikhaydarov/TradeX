name: Build check

on:
  push:
    branches:
      - main
      - agent/app-router-ui-parity-v2
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Upload refactor sources
        uses: actions/upload-artifact@v4
        with:
          name: refactor-main-source
          path: |
            src/components/feed-v3.tsx
            src/components/feed
            src/components/journal-v2.tsx
            src/components/journal
            src/components/sidebar.tsx
            src/components/workspace-app-router-shell-v2.tsx
            src/app/(workspace)
          if-no-files-found: warn
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Verify UI parity scope
        run: npm run check:ui-parity
      - name: Lint
        run: npm run lint
      - name: Build and capture log
        id: build
        shell: bash
        run: |
          set +e
          npm run build > build.log 2>&1
          status=$?
          echo "status=$status" >> "$GITHUB_OUTPUT"
          tail -n 160 build.log
          exit 0
      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: next-build-log
          path: build.log
          if-no-files-found: error
      - name: Fail when build failed
        if: steps.build.outputs.status != '0'
        run: exit 1
